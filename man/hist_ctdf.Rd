% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{hist_ctdf}
\alias{hist_ctdf}
\title{Histogram of observation times by cluster}
\usage{
hist_ctdf(ctdf, binwidth = 3 * 3600, static = FALSE)
}
\arguments{
\item{ctdf}{A \code{ctdf} object.}

\item{binwidth}{Bin width in seconds (POSIXct is binned in seconds). Defaults to 3 hours.}

\item{static}{Logical. If \code{TRUE}, return a static \code{ggplot}; otherwise return an interactive \code{plotly} object.}
}
\value{
A \code{ggplot} (if \code{static = TRUE}) or a \code{plotly} htmlwidget (if \code{static = FALSE}).
}
\description{
Plot per-cluster histograms of observation times with cluster start/stop markers,
stacked vertically. Returns either a static \code{ggplot} or an interactive \code{plotly} object.
}
\examples{
require(clusterTrack.Vis)
data(pesa56511)
ctdf = as_ctdf(pesa56511, time = "locationDate") |> cluster_track()
hist_ctdf(ctdf)
}
